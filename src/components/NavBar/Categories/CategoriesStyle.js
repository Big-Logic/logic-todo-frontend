import styled from "styled-components";

export const Header = styled.h2`
  margin: 2rem 0 1rem 0;
  padding: 0.6rem 1rem;
  font-size: var(--header-font);
`;

export const CategoriesCont = styled.ul`
  list-style: none;
  min-height: 200px;
  border-radius: var(--rounded-10);
`;
